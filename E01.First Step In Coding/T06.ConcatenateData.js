function concatanateData(input) {
    
    let firstName = input[0];
    let secondtName = input[1];
    let age = Number(input[2]);
    let city = input[3];

    console.log(`You are ${firstName} ${secondtName}, a ${age}-years old person from ${city}.`);
}